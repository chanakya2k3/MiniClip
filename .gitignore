node_modules/
*.log
npm-debug.log*
.env
.env.local

# SQLite database + its write-ahead log. User data and session rows — never
# commit these. .env.example documents the variables; this data is local.
data/
*.db
*.db-wal
*.db-shm
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local editor/agent tooling config. settings.local.json holds per-machine
# permission grants, and launch.json is an auto-generated stub — neither is
# project configuration anyone else needs.
.claude/
